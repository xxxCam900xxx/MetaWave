FROM python:3.12-slim

RUN apt-get update && apt-get install -y ffmpeg

WORKDIR /app

COPY app/metawave_server/downloader/requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

COPY app/metawave_server/downloader/update_playlist.py .

# Optionale Cookie-Datei für authentifizierte YouTube-Requests.
# Wenn in der Build-Context-Datei
#   app/metawave_server/downloader/www.youtube.com_cookies.txt
# vorhanden ist, wird sie ins Image kopiert und als Standard für
# YT_COOKIES gesetzt.
COPY app/metawave_server/downloader/www.youtube.com_cookies.txt /cookies/www.youtube.com_cookies.txt
ENV YT_COOKIES=/cookies/www.youtube.com_cookies.txt

VOLUME ["/songs"]

CMD ["python", "update_playlist.py"]