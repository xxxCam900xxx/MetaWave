# Development Enviroment

1. Erstelle `.env` in Ordner `/metawave_server`

2. Fülle `.env` mit folgendem
```bash
NGROK_AUTHTOKEN=<DEIN_NGROK_TOKEN>
```

3. Starte mit `docker compose -f ./compose.server.yaml` up --build` die Container

Viel Spass beim Developen!